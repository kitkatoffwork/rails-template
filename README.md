# Rails Template Repository

## Development

Execute the following commands after cloning

```
docker-compose build
docker-compose up -d
docker-compose exec app bash
rails db:create
```

Access localhost:3000
